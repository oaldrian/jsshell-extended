# edit

Interactive text editor.

## Usage

- `edit <path>`

## Notes

- Paths with spaces must be quoted (example: `edit "My Notes.txt"`).

- Installed as `/bin/edit.js`.

## Tips

- Use `cat` to quickly view a file.
- Use `viewmd` to render Markdown.

## See also

- `cat`, `viewmd`, `touch`