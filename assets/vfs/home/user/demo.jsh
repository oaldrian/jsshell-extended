# demo.jsh - Example JsShell script file
#
# A .jsh file contains one program call per line.
# It must be invoked explicitly (e.g. ./demo.jsh).
# Lines are executed in order and execution stops on the first error.

pwd
ls
cd /bin
./sample.js Alice
cd /home/user
date
